---
layout: default
title: "Photo Gallery"
menu_show: true
permalink: /:collection/fg
---

Photos from previous PECS Editions! (the gallery is under construction)

<style>
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 20px;
    margin-top: 20px;
  }
  .gallery-item {
    text-align: center;
    background: #f9f9f9;
    padding: 10px;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  }
  .gallery-item img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    border-radius: 4px;
    display: block;
    margin-bottom: 8px;
  }
  .image-title {
    font-size: 0.9em;
    font-weight: bold;
    color: #333;
    word-wrap: break-word;
  }
</style>

<div class="gallery-grid">
{% for file in site.static_files %}
  {% if file.path contains '/photos/' %}
    <div class="gallery-item">
      {% comment %} 
        Estraiamo il nome del file:
        1. Dividiamo il path con '/' e prendiamo l'ultima parte
        2. Dividiamo il risultato con '.' e prendiamo la prima parte
      {% endcomment %}
      {% assign filename = file.path | split: '/' | last | split: '.' | first %}
      
      <a href="{{ file.path | relative_url }}" target="_blank">
        <img src="{{ file.path | relative_url }}" alt="{{ filename }}" />
      </a>
      <div class="image-title">{{ filename | replace: "-", " " | replace: "_", " " | capitalize }}</div>
    </div>
  {% endif %}
{% endfor %}
</div>

---
<!-- -->






